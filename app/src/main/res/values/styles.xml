<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/colorPrimaryDark</item>
        <item name="android:windowTitleSize">30sp</item>
        <!--<item name="checkedTextViewStyle"> #corPadraoDasFontes</item>-->
    </style>

    <style name="CustomizaEditText" parent="@android:style/TextAppearance">
        <item name="android:textColorHighlight">@color/corBrancaFonte</item>
        <item name="android:textColorHint">@color/corHintCinzaEscuro</item>
        <item name="android:layout_height">50dp</item>
        <item name="android:layout_marginTop">60dp</item>
        <item name="android:layout_marginRight">15dp</item>
        <item name="android:layout_marginLeft">15dp</item>
    </style>


    <style name="Theme.AppCompat.Light.NoActionBar" parent="@style/Theme.AppCompat.Light">
        <item name="windowNoTitle">true</item>
    </style>

    <style name="CustomizaBotaoPadrao" parent="Widget.AppCompat.Button.Borderless">
        <item name="android:layout_gravity">center_horizontal</item>
        <item name="android:textSize">18sp</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:background">@drawable/shape_botao</item>
    </style>

</resources>
